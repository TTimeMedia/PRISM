# auth

Sign up, sign in, forgot/reset password, email verification. See docs/SCREEN_BIBLE.md §4 and docs/MASTER_BUILD_SPEC.md §17.

Built in Milestone 02 (Authentication & Identity). Routed from `app/(auth)/`; session/recovery state lives in `lib/auth/AuthProvider.tsx`.
